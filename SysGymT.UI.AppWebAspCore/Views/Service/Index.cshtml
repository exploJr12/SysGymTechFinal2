@model IEnumerable<SysGymT.EntidadesDeNegocio.Service>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name_Service)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descryption)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name_Service)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descryption)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Service">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Service">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Service">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
